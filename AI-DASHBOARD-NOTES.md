# viz 状態キャプチャが想定するダッシュボード(制約メモ)

このフォークの viz 状態スナップショット機能(概要は [README.md](README.md) 参照)が、
どんなダッシュボードを想定して設計されているかを記録するメモ。

**これは設計規範(ガイドライン)ではない。** 内容はすべて Embedding API v3 と
VizQL Data Service(VDS)の制約 — 大半は実測で確認済み — からの導出であり、
MCP Apps ホスト上での実運用経験を反映したものではない(ホスト側 CSP 制約により
E2E 検証は未実施)。運用知見が得られたら書き直される前提の文書である。

---

## 前提: API が読ませてくれるもの・くれないもの

スナップショットが確実に読み取れるのは次の 4 つに限られる:

- フィルター値(カテゴリ/範囲/相対日付)
- パラメーター値
- 選択マーク
- アクティブシートの要約データのサンプル(行数上限あり)

逆に、以下は Embedding API v3 が読み出しを提供しておらず、原理的に取得できない:

- **ハイライト状態** — 読み出し API が存在しない
- **画面上のソート順** — ヘッダーのソート操作は要約データに反映されない(実測)
- **マップのズーム/パン** — 表示範囲を取得できない
- **セットアクションのセッション状態** — 選択に応じてセットが更新されても、その中身は読めない
- **ストーリーポイント** — ストーリーは本機能の対象外
- **サーバー側フィルタリング** — データソースフィルター、行レベルセキュリティ(RLS)、
  抽出フィルターはユーザーにも不可視であり、スナップショットにも現れない。AI が見ている
  数字はすでにこれらが適用された後のもの、という前提で扱う

読み取れない状態は「不具合」ではなく「対象範囲の定義」である。

深掘り(スナップショットを超える再集計)の経路にも制約がある:

- 再クエリの主経路は `query-workbook-datasource`(VDS のセッション方式)。スナップショットが
  運ぶデータソース id と viz のセッション値をそのまま使うため、**公開データソースだけでなく
  ワークブック埋め込みデータソースも再クエリできる**(LUID 経由の `query-datasource` は
  公開データソース限定のまま)
- **VDS から見えるのはデータソースに定義されたフィールドのみ**。シートローカルの
  計算フィールド、パラメーターを参照する計算フィールドは見えない

---

## この制約が想定するダッシュボード像

上の制約をそのまま裏返すと、この機能が最も忠実に働くダッシュボードの姿が決まる。

### 状態がフィルターとパラメーターで表現されている

読み取れる状態はフィルター値・パラメーター値・選択マークの 3 つなので、
セッション内にしか存在しない仕掛けで状態を表現すると AI からは見えない。

例: 「上位 10 製品に絞る」をセットアクションのハイライト切り替えで表現すると、
AI からは何も絞られていないように見える。`Top N` パラメーター + それを参照する
フィルターなら、スナップショットに `Top N = 10` として現れる。

同様に、画面上のソート操作は状態として読み出せないため、「売上順の順位」に分析的な
意味があるなら `RANK(SUM([Sales]))` のような順位計算フィールドとしてデータに実体化
されていることを想定している。そうなっていれば順位は要約データの列として乗る。

### 公開データソースを参照している(推奨。ただし必須ではなくなった)

スナップショットは要約データのサンプルしか含まないので、それを超える分析は VDS での
引き直しになる。セッション方式(`query-workbook-datasource`)により、ワークブック埋め込み
データソースでも「別の切り口で集計し直して」に答える経路は**存在するようになった**。

それでも公開データソースを推奨する理由は残る: セッション非依存の経路(`query-datasource`)が
併用でき、viz を開いていない文脈でも引ける。ガバナンス(API Access パーミッション、
データソース許可リスト)も公開データソース単位でしか効かない。

### 分析計算がデータソース側にある

例: 「利益率」がワークブック内の計算フィールドだと、AI は再クエリで利益率を取れない
(スナップショットに写っていれば読めるが、別カットでは計算できない)。データソース側に
`Profit Ratio` が定義されていれば、任意のディメンションで引き直せる。

なお、**パラメーターの「値」自体はスナップショット経由で AI に届く**。届かないのは、
そのパラメーターを使った計算を VDS 側で再現する能力のほう。閾値パラメーターの現在値を
AI に伝えたうえで、再クエリはデータソース側のフィールドで組み立てる、という分担になる。

---

## 想定から外れたダッシュボードでも動く

上記の想定に合わないダッシュボードでも、状態キャプチャそのものは働く。フィルター・
パラメーター・選択マーク・要約データのサンプルは同じように取得される。

劣化するのは**データ再現の忠実度**だけ:

- スナップショットに写っている範囲の質問には答えられる
- 別の切り口・別シート・打ち切られた行の先を引き直そうとしたときに、必要な計算フィールドが
  データソース側に見えない、画面の状態(セッション内の仕掛け)を再現できない、という形で
  行き止まりになる(「公開データソースが無い」はセッション方式の導入により
  行き止まりではなくなった)

つまりこの想定は「動く/動かない」の境界ではなく、**AI がどこまで深掘りできるかの境界**を
決めている。
